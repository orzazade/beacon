# Beacon AI Infrastructure Configuration
# NOTE: This file is for the standalone docker-compose (documentation only)
#
# Primary setup uses dev-stacks at ~/Projects/dev-stacks with:
#   - pgvector: localhost:5432 (user: admin, pass: secret)
#   - ollama: localhost:11434

# Standalone Beacon Stack (if not using dev-stacks)
PGVECTOR_DB=beacon
PGVECTOR_USER=beacon
PGVECTOR_PASS=beacon_secret
PGVECTOR_PORT=5433

OLLAMA_PORT=11435

# OpenRouter API key is stored in macOS Keychain (not here)
